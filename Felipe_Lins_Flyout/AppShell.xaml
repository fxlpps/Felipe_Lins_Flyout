<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="Felipe_Lins_Flyout.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:Felipe_Lins_Flyout"
    Shell.FlyoutBehavior="Flyout"
    Title="Felipe_Lins_Flyout">

    <FlyoutItem Title="Início">
        <ShellContent
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage" />
    </FlyoutItem>

    <FlyoutItem Title="Dipper">
        <ShellContent 
        ContentTemplate="{DataTemplate local:Dipper}"
        Route="Dipper"/>
    </FlyoutItem>

    <FlyoutItem Title="Bill Cipher">
        <ShellContent 
        ContentTemplate="{DataTemplate local:BillCipher}"
        Route="BillCipher"/>
    </FlyoutItem>

    <FlyoutItem Title="Stanford Pines">
        <ShellContent 
    ContentTemplate="{DataTemplate local:StanfordPines}"
    Route="StanfordPines"/>
    </FlyoutItem>

</Shell>
